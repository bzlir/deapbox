[package]
name = "deapbox-store"
version = "0.1.0"
edition = "2021"

[dependencies]
deapbox-core = { path = "../deapbox-core" }
serde = { version = "1", features = ["derive"] }
toml = "0.8"
thiserror = "2"

[dev-dependencies]
tempfile = "3"
